## 📌 Como rodar o projeto?

### 🔧 Instale o TypeScript e inicialize o projeto:
```sh
npm init -y
npm install typescript ts-node @types/node
npx tsc --init

🚀 Para executar o projeto:
Você pode rodar o projeto diretamente sem precisar compilar, usando ts-node:

sh
Copiar
Editar
npx ts-node src/index.ts
🔄 Para compilar e executar o projeto:
Caso queira compilar os arquivos TypeScript antes de rodar o código, utilize os comandos abaixo:

sh
Copiar
Editar
npx tsc
node dist/index.js
nginx
Copiar
Editar

Esse formato vai garantir que sua explicação fique bem clara e organizada no **GitHub**! 🚀😃
